#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(){
    int i, j, maior = 0, coordenada[1], C[10][15];
    for(i=0;i<10;i++){
        for(j=0;j<15;i++){
            printf("Digite um valor para a Matriz\n");
            scanf("%d", &C[i][j]);

        }
    }
    printf("O maior valor é %d na coordenada %d %d", maior, coordenada[0], coordenada[1]);
    return 0;
}