#include <stdio.h>
#include <stdlib.h>
#include <math.h.>

int pi(int n) {
    
}

int main(){
    int n;
    printf("Write a number for pi:\n");
    scanf("%d", &n);
    printf("The pi number is %d", pi(n));
}